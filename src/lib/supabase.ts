// Supabase has been replaced by the local Express + JWT backend.
// This stub is kept so any legacy imports don't break at runtime.
// Authentication is handled via /api/auth/* routes and localStorage JWT tokens.

export const supabase = null as any
